{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 .flutter-sdk && export PATH=\"$PATH:$(pwd)/.flutter-sdk/bin\" && flutter config --no-analytics && flutter precache --web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
